Requires a database structure to work with,

Database name: Pwmanager
Table name: users
Table Structure : uid - primary key
                  username varchar(40) not null unique
                  password varchar(40)
                  status int
                  
